language: python

python:
  - "2.6"
  - "2.7"
  - "3.4"
  - "3.5"

env:
  - ROBOT_INTERNAL_TRACES=yes


install:
  - "pip install -r requirements.txt"

script:
  - touch debug.log
  - pybot -P src -L DEBUG -b debug.log tests
  - cat debug.log
